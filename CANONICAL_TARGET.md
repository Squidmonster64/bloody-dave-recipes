# Canonical implementation target

This Cloud Agent environment is attached to `Squidmonster64/bloody-dave-recipes` (placeholder) and **cannot push** to `Squidmonster64/bloody-recipes`.

The Recipe Studio brief requires implementation in **`Squidmonster64/bloody-recipes`**.

This branch contains the full `bloody-recipes` tree plus Recipe Studio so the work can be reviewed and transferred:

1. Prefer: grant the agent write access to `bloody-recipes` and re-open/push this branch there.
2. Or: manually copy/merge these commits into `bloody-recipes` and open the PR against that repo's `main`.

Do not treat `bloody-dave-recipes` as the live library host.
